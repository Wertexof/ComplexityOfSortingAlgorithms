#ifndef SORTS_PROJECT_GENERATORS_H
#define SORTS_PROJECT_GENERATORS_H

#include <stddef.h>

void generateRandomArray(int *a, size_t n);

void generateOrderedArray(int *a, size_t n);

void generateOrderedBackwardsArray(int *a, size_t n);

#endif
